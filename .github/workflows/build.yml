name: Build & Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

# Workflow-level default is read-only. Each build job widens to contents:write
# only because it uploads its installer to the release tag. Splitting upload
# into a separate job is a deferred follow-up (see CLAUDE.md CI/CD section).
permissions:
  contents: read

jobs:
  build-mac:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 (pin)
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4 (pin)
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Verify npm package signatures
        run: npm audit signatures
      - name: Build, sign & notarize macOS
        run: npm run build:mac
        env:
          # Apple Developer ID code signing certificate (base64-encoded .p12) + password.
          # electron-builder reads CSC_LINK / CSC_KEY_PASSWORD automatically.
          CSC_LINK: ${{ secrets.MAC_CERT_P12_BASE64 }}
          CSC_KEY_PASSWORD: ${{ secrets.MAC_CERT_PASSWORD }}
          # Apple notary service credentials (used by electron-builder when notarize: true).
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2 (pin)
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: |
            dist/*.dmg
            dist/*.zip

  build-win:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 (pin)
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4 (pin)
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Verify npm package signatures
        run: npm audit signatures
      - run: npm run build:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2 (pin)
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: dist/*.exe

  build-linux:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4 (pin)
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4 (pin)
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Verify npm package signatures
        run: npm audit signatures
      - run: npm run build:linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2 (pin)
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: dist/*.AppImage
