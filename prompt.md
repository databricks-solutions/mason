I want to build a new feature for Mason. Make sure to review the claude.md for what this project is and where we are. I have created a new branch off main to create this feature. I want to allow mason to be an app where users can also build applications on their local machine. Here is how I envision it. Just above the 'workflow designer' button, we'll have another button with the same size and layout but it will be 'app builder'. Once the app builder button is clicked the UI is split into three different panes, on the right we'll have the chat interface, on the far left we'll have the current working directory file tree, and in the center we'll have a local host window pane of the app running. Users will be able to prompt in the chat window on the right, and the ai will make changes to the files on the left, and the UI will reflect the changes of the application. Create a spec sheet that an Opus agent will implement and Codex will review. Interview me for clarity on this ask as well. 