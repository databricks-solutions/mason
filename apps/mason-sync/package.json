{
  "name": "mason-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Session-sync server for Mason — Databricks App with Lakebase persistence and a mobile web viewer",
  "main": "build/src/server.js",
  "scripts": {
    "build": "node scripts/copy-shared.js && tsc && tsc -p tsconfig.viewer.json",
    "start": "node build/src/server.js",
    "dev": "npm run build && MASON_SYNC_STORE=memory MASON_SYNC_INSECURE_DEV=1 node build/src/server.js",
    "test": "npm run build && node --test 'build/test/**/*.test.js'"
  },
  "dependencies": {
    "@fastify/static": "9.1.3",
    "fastify": "5.8.5",
    "pg": "8.21.0"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "@types/pg": "8.20.0",
    "typescript": "5.9.3"
  }
}
